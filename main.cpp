#include <iostream>
#include <thread>
#include <mutex>
#include <atomic>
#include <future>
#include <algorithm>
#include <cassert>

#include <unistd.h>

// std::thread
// std::mutex;
// std::recursive_mutex;
// std::lock_guard
// std::unique_lock
// std::atomic
// std::atomic_flag

// std::future
// std::promise
// std::async
// std::package_task
// std::async

int main()
{
}